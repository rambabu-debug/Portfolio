// Export as an ES module because the project uses "type": "module" in package.json
// PostCSS supports ESM config when exporting a default object.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}

